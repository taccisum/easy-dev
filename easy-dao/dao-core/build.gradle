dependencies {
    compile project(':easy-core')
    compile(
            [group: 'javax.persistence', name: 'persistence-api', version: '1.0'],
            [group: 'tk.mybatis', name: 'mapper', version: '3.4.4'],
    )
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-databind'
}
